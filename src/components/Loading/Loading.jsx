import React from "react";
import { LoadingLine, LoadingWrapper } from "./Loading.elements";

function Loading() {
    return (
        <LoadingWrapper>
            <LoadingLine></LoadingLine>
        </LoadingWrapper>
    );
}

export default Loading;
